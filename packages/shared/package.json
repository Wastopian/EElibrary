{
  "name": "@ee-library/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/asset-state.test.ts src/normalization.test.ts src/exact-mpn.test.ts src/detail-view-model.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
