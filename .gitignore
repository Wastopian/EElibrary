# Dependency directories.
node_modules/

# Build and framework outputs.
.next/
dist/
coverage/

# Local scratch directory for dev/test logs and fixtures.
tmp/

# Local environment files.
.env
.env.local
.env.*.local

# Team server secrets, host-mounted data, and backup archives (never committed).
.env.team
team-data/
backups/

# Operating-system and editor noise.
.DS_Store
Thumbs.db
*.log
*.tsbuildinfo

# Claude Code worktree metadata and per-session checkouts.
.claude/

# Docker Desktop "Dev Environments" metadata (auto-generated, machine-local).
compose-dev.yaml

# Worker-local file storage (downloaded datasheets/CAD bytes; dev data, never committed).
apps/worker/storage/

# Stray Chromium automation-profile debris: a browser launched with its user-data-dir
# pointed at the repo root leaves these. Safe to delete on sight.
Crashpad/
CrashpadMetrics*.pma
Default/
GPUPersistentCache/
GrShaderCache/
ShaderCache/
component_crx_cache/
extensions_crx_cache/
segmentation_platform/
Local State
Last Version
Variations
Safe Browsing/
