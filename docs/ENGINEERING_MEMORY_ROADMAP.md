# Engineering memory and roadmap archive

This document captures **what is done**, **how the merged product is operated**, and **where to look next**. It intentionally avoids duplicating tables that already live in [`IMPLEMENTATION_STATUS.md`](./IMPLEMENTATION_STATUS.md).

- **Active backlog only:** [`TODO.md`](../TODO.md) in the repo root.
- **Completed FUNC / backlog narratives (frozen):** [`docs/TODO_COMPLETED_ARCHIVE.md`](./TODO_COMPLETED_ARCHIVE.md).
- **Dated worktree / integration notes:** [`REGISTER.md`](../REGISTER.md).
- **Agent / repo rules:** [`AGENTS.md`](../AGENTS.md).

---

## Repo integration note (2026-05-03)

`main` was previously the slimmer **phase-2-foundation** line (catalog search/detail + worker ingest) after PR #1. The full **engineering-memory** implementation lived on branch `cursor/2026-05-03-7o4u-787f9` (tip `d559ca4`), including the FUNC-numbered backlog now preserved in [`docs/TODO_COMPLETED_ARCHIVE.md`](./TODO_COMPLETED_ARCHIVE.md). There were **no git stashes** and **no extra linked worktrees**; only stale metadata could have suggested otherwise.

Those histories were merged at their common base (`3a0d598`). **`main` now contains the full stack** (projects, BOM memory, circuit blocks, evidence vault, export bundles, provider import, admin queues, etc.). If anything still looks missing locally, run migrations and scripts from [`README.md`](../README.md).

---

## How an engineer uses the site today (summary)

Typical flow (details and commands in [`README.md`](../README.md)):

1. **Catalog** — Open `/` or `/catalog`: dense search, filters (readiness, approval, CAD, lifecycle, connectors, …), exact-MPN provider import when there is no internal match.
2. **Part detail** — `/parts/[partId]`: answer-first readiness, datasheet and CAD/export state, connector buildable set, provenance, admin-aligned actions.
3. **Project memory** — `/projects`: create projects and revisions, CSV/XLSX BOM import and mapping, match rows to catalog parts, BOM diagnostics, revision compare, fleet risk on the dashboard, follow-ups, export bundle history and downloads where file-backed.
4. **Cross-cutting** — `/where-used`, `/circuit-blocks`, `/evidence`, `/admin`, `/system/health` (see implementation matrix).

Honesty rules (export gating, weak vs matched BOM lines, evidence vs approval) are documented in [`IMPLEMENTATION_STATUS.md`](./IMPLEMENTATION_STATUS.md) and enforced in API/store code—not re-stated here.

---

## Delivered milestones (pointer)

Shipped vs foundation vs planned is maintained in:

- [`docs/IMPLEMENTATION_STATUS.md`](./IMPLEMENTATION_STATUS.md) — source-of-truth matrix for code vs docs.
- [`TODO.md`](../TODO.md) — **active** backlog only.
- [`docs/TODO_COMPLETED_ARCHIVE.md`](./TODO_COMPLETED_ARCHIVE.md) — frozen FUNC1–FUNC18 narratives and related completion prose from the former root `TODO.md`.

When you **finish** work: update [`TODO.md`](../TODO.md) (remove or tighten items), update [`IMPLEMENTATION_STATUS.md`](./IMPLEMENTATION_STATUS.md), and add a **short dated bullet** under **Archive log** below only if you need narrative beyond the matrix. Large historical FUNC write-ups remain in the completed archive.

### Archive log

- **2026-05-03** — Merged engineering-memory branch into `main`; restored root `TODO.md`; marked **FUNC14** (lifecycle regression / `lifecycle_risk_changed` BOM health finding) complete in `TODO.md` to match `apps/api/src/project-memory-store.ts` and tests.
- **2026-05-04** — Closed the original engineering-memory FUNC backlog by shipping the last two items.
  - **FUNC15 (Connector Set Catalog View)** added the `/connector-sets` workspace, a typed `ConnectorSetListResponse` plus mate-pair types, the `GET /connector-sets[?connectorClass=…&q=…]` API route, and `readConnectorSetCatalogFromDatabase` in `apps/api/src/project-memory-store.ts`. Reuses `parts`, `manufacturers`, `part_readiness_summaries`, `part_approvals`, `mate_relations`, and an aggregated `project_part_usages` count — no new schema. Pg-mem fixture extended with a `mate_relations` table and a JST connector seed; tests cover groups, mate-pair counts, confidence values, the class/query filters, and the boundary copy.
  - **FUNC16 (Approval Batch Workflow From Project BOM Context)** added the project-scoped approval queue (`GET /projects/:projectId/approval-candidates`) and admin-gated bulk action (`POST /projects/:projectId/approval-batch`), backed by `readApprovalBatchCandidatesFromDatabase` and `applyApprovalBatchInDatabase`. Approval state is the only field changed; readiness summaries, assets, and part issues stay untouched. Project context is recorded in the `evidence` array (`project:<id>`, `project_key:<key>`, `triggered_by:approval_batch`, `decided_by:<actor>`). New `ApprovalBatchPanel` mounted on the project detail page with select-all/clear, action selector, optional notes, and per-part outcome summary. Tests cover the candidates query, the bulk action with `applied`/`skipped_already_approved`/`not_found` outcomes, and validation for empty `partIds`, an unsupported action, and a missing project.
  - With FUNC1 through FUNC16 now complete, the original engineering-memory roadmap has shipped. The next planning wave starts from this baseline.
- **2026-05-04 (doc pass)** — Synced `docs/IMPLEMENTATION_STATUS.md`, `README.md`, and `docs/ROADMAP.md` with the shipped surface so public docs no longer describe matching, where-used, BOM health, evidence vault, circuit blocks, connector catalog, or approval batch as “planned” only. [`TODO.md`](../TODO.md) Project Review Findings updated accordingly; next build priorities point at asset preview and compare (`AGENTS.md`).
- **2026-05-03 (README + backlog alignment)** — Corrected [`README.md`](../README.md) (basic `/compare` shipped and in nav; `/tools` still absent). Updated [`docs/IMPLEMENTATION_STATUS.md`](./IMPLEMENTATION_STATUS.md) foundation row for compare/tools vs nav. Rewrote root [`TODO.md`](../TODO.md) into goal-aligned sections (doc truth, AGENTS build order, README-named extensions, usability).
- **2026-05-05** — Shipped **FUNC17** (inline **stored PDF** preview on part detail when `previewStatus` is ready and the file is local) and **FUNC18** ( **`/compare`** for up to four parts, `buildCompareUrl`, sidebar nav, part-detail link, `part-compare` helpers + tests). Deeper non-PDF / 3D preview and rich connector-side-by-side compare remain future work; see `IMPLEMENTATION_STATUS.md` “Foundation and Partial” rows.
- **2026-05-05 (backlog split)** — Migrated the long “done” narrative from root `TODO.md` to [`docs/TODO_COMPLETED_ARCHIVE.md`](./TODO_COMPLETED_ARCHIVE.md). Root [`TODO.md`](../TODO.md) is **active tasks only**; use the archive for FUNC history and the matrix for shipped truth.
- **2026-05-05 (asset preview + compare depth — first increments)** — Continued the `AGENTS.md` priority list past export hardening + trust lineage to the next two items.
  - **Asset preview** extended `AssetInlinePreview` from a silent-when-not-embeddable iframe into a five-state matrix encapsulated in `getAssetPreviewState`: `stored_pdf_inline`, `pdf_reference_only`, `ready_unsupported_format` (STEP/KiCad/DXF/unknown that the catalog claims preview-ready), `preview_pending` (queue copy with download still working), and `preview_not_available` (returns null — the AssetCard badge already says "No preview"). New tests cover all five paths and exhaustively iterate the `FileFormat` and `AssetAvailabilityStatus` enums so the helper stays honest as the catalog grows.
  - **Deeper compare** added two new sections to `/compare`. `buildCompareAssetClassRows` emits one row per engineering asset class (Symbol / Footprint / 3D / Datasheet / Mechanical drawing) with per-part readiness badges; missing classes render "Missing" rather than disappearing. `buildCompareConnectorRows` emits six connector-depth rows (class, best mate w/ confidence, alternate-mate count, required-accessory count, family conflicts, mating confidence). `shouldRenderConnectorCompareRows` keeps the connector section out of pure non-connector compares; mixed compares render an em dash on non-connector parts so the table stays readable. New `CompareCellTable` component renders rows with shared tone-aware badges. Tests cover row order, missing-class honesty, the connector vs non-connector trigger, em-dash behavior on non-connector cells, and the persisted-confidence cell.
  - Remaining work is captured in [`TODO.md`](../TODO.md) §2.3–2.4 (real 3D/STEP preview pipeline, image format support; side-by-side CAD preview, per-asset trust diff, deeper datasheet-revision diff).
- **2026-05-05 (export hardening + trust lineage — first increments)** — Took the first bites out of the top two `AGENTS.md`-priority items.
  - **Export path hardening** added an `ExportBundleFileAvailability` typed field (`available` | `manifest_only` | `file_missing`) to the `ExportBundle` shared contract. The store now resolves availability at read time by asking the `FileStorageClient` whether the bundle’s `storageKey` is present; missing files surface as “File missing” copy in `ExportBundlePanel` instead of a broken Download link, and the manifest-row warnings explicitly call the regenerate path. `FileStorageClient` gained an `exists(storageKey)` method (local + not-configured backends both implement it; worker and API test stubs updated). New unit test in `apps/api/src/project-memory-store.test.ts` (`resolveExportBundleFileAvailability`) covers the manifest-only / available / file-missing / throwing-storage paths. Bundle file generation itself is still foundation; the contract is now ready for it.
  - **Validation and trust surfacing** added a four-stage **Trust lineage strip** on part detail (`apps/web/src/lib/trust-lineage.ts` + tests; `TrustLineageStrip` component in `apps/web/src/app/parts/[partId]/page.tsx`; CSS in `apps/web/src/app/globals.css`). The strip exposes `imported → reviewed → approved → verified_for_export` with explicit per-stage state (`passed` / `pending` / `blocked` / `not_applicable`) plus a one-line reason and the boundary copy “none of the earlier gates implies the next”. Tests cover canonical stage order, source-import vs failed/empty pathways, review-record vs approval separation, full PartApproval status mapping, and the verified-for-export gate (including the `ready to promote` pending state).
  - Remaining work for both: see refreshed entries in [`TODO.md`](../TODO.md) §2.1–2.2.
- **2026-05-05 (export hardening + trust lineage — second increments complete)** — Closed the two then-top active backlog items and moved them out of active TODO.
  - **Export path hardening (continued)**: `createExportBundleInDatabase` now writes deterministic archive payloads via storage when available, the bundle record persists `storageKey` on success with `fileAvailability: available`, storage write failures are surfaced as manifest warnings in the bundle row, and `/storage/:key` now requires admin auth.
  - **Validation and trust surfacing (continued)**: compact trust context now appears in admin review/promotion queues and project usage rows so operators can scan trust gates outside part detail without blurring approval vs verified-for-export.
  - Active backlog now carries only remaining follow-on tasks (worker-side bundle byte assembly, deeper preview/compare/tooling).
- **2026-05-05 (catalog trust gates)** — Search **table and list** rows now render four abbreviated lineage badges (`Imp`, `Rev`, `Apr`, `Exp`) with explicit state marks (`OK`, `…`, `!`, `n/a`) and tooltips, wired through `buildCatalogTrustLineageBadges` / `getTrustLineageSummaryForSearchRecord` in `apps/web/src/lib/trust-lineage.ts`. Test runner now discovers `.test.tsx` files (`scripts/run-tests.mjs`).
- **2026-05-06 (asset preview + compare depth — second increments)** — Continued the next two active AGENTS-priority items.
  - **Asset preview** now treats image formats as first-class (`png`/`jpg`/`jpeg`/`webp`) in shared `FileFormat`, adds inline stored-image preview states in `AssetInlinePreview`, and updates storage serving MIME handling for image inline responses. Worker persistence now enforces an honesty guard: `previewStatus = ready` is written only when the file is embeddable and locally stored (`downloaded`/`validated` + `storageKey`), otherwise it is downgraded to `not_available`.
  - **Deeper compare** now includes a dedicated **Per-asset trust-stage diff** section in `/compare` via `buildCompareAssetTrustRows`, so generated/approved/verified status differences are visible by asset class side-by-side instead of inferred from aggregate badges.
  - Remaining scope is narrowed to 3D/STEP artifact+viewer preview and side-by-side CAD / richer datasheet revision compare depth.
- **2026-05-06 (usability pass: compare flow + project usage readability)** — First usability-focused follow-up after README/workspace audit.
  - Added **CompareSelectionTray** on `/compare` with in-page add/remove controls so operators can build a compare set without manually editing query strings.
  - Catalog result rows now include direct compare actions (`Compare` / `Add to compare`) linked through `buildCompareUrl`.
  - Project detail `Confirmed usage` rows now show **MPN + manufacturer** as primary identity with internal part id retained as secondary metadata.
- **2026-05-07 (usability pass: evidence + where-used recovery)** — Closed the next two workstation UX items: `/evidence` attach now offers a searchable persisted-target picker with explicit ID override preserved, and `/where-used` now includes target-specific query examples plus no-result recovery hints.
- **2026-05-08 (export bundle pipeline — worker-side asset-byte assembly)** — Took the next active backlog item past the manifest-only foundation. `export_bundles` gained `assembly_status` (`not_required` | `pending` | `assembled` | `assembly_failed`), `assembly_error` JSONB, `assembly_completed_at`, and `assembly_attempt_count` columns (migration `031_export_bundle_assembly.sql`); `FileStorageClient` gained a `read()` method so the worker can fetch verified asset bytes; `createExportBundleInDatabase` now queues `pending`/`not_required` instead of conflating manifest write with byte copy; new module `apps/worker/src/export-bundle-assembly.ts` plus the `assemble-bundles` CLI command read pending bundles, copy each verified asset's bytes into `export-bundles/<projectId>/<bundleId>/assets/<bundlePath>`, and persist structured failure telemetry (`phase`, `failedAssetId`, `failedBundlePath`, `message`, `failedAt`) on per-asset failures. `ExportBundlePanel` surfaces the assembly state column and inline phase-aware copy. Tests cover the assembled, zero-asset, fetch_asset failure, and write_asset failure paths plus the batch persistence updates.
- **2026-05-08 (export bundle pipeline — single-archive download + workstation friction pass)** — Closed the bundle download story and the workstation friction items surfaced during the usability review. `apps/worker/src/tar-archive.ts` adds a small POSIX ustar writer + deterministic gzip helper (no third-party dep); the worker now writes `bundle.tar.gz` at a deterministic `archive_storage_key` after successful assembly (migration `032_export_bundle_archive_key.sql`); the shared `ExportBundle` contract gained `archiveStorageKey` + `archiveAvailability`, both resolved at read time against `FileStorageClient.exists` so a "Download archive (.tar.gz)" link is offered only when the file is actually present; `ExportBundlePanel` exposes the archive download alongside the manifest download with honest `Archive missing` / `Manifest only` fallback states. Friction fixes shipped alongside: `npm run lint` no longer fails at the root because `@ee-library/db` now lints via `tsc --noEmit` (matching the rest of the workspaces); `/projects` setup-required no longer stacks a redundant technical `QUERY_FAILED` line above the setup panel when the database is unreachable (the line stays for the genuinely-unexpected DB-up-but-API-failing case); `npm run setup:dev` now pre-flights `docker --version` and `docker info` so a missing or stopped Docker daemon produces actionable copy with an install link instead of an opaque ENOENT.
- **2026-05-08 (export bundle pipeline — UI auto-refresh while assembly is pending)** — Closed the last UX gap in the bundle generation loop. `ExportBundlePanel` now polls `fetchProjectExportBundles` every 8s while at least one bundle is in `pending` assembly and stops polling once every row has settled (`assembled` / `assembly_failed` / `not_required`). A "Refreshing assembly status…" indicator appears beside the Bundle history heading while polling is active. The polling decision is extracted as `shouldAutoRefreshBundleAssembly` (pure helper) so the rule is unit-testable without a DOM. With this in place an operator who clicks Generate sees the row flip from `Assembling` to `Assembled` (with a "Download archive (.tar.gz)" link) within one daemon cycle, no manual reload required.
- **2026-05-08 (export bundle pipeline — daemon scheduling)** — Closed the last remaining piece of TODO §2.3. `runDaemon` in `apps/worker/src/index.ts` now schedules `processPendingExportBundleAssembly` on a 30s tick (independent from the heartbeat interval) and runs one pass at startup so a bundle queued while the daemon was offline does not have to wait a full interval. Each tick is wrapped in `safeProcessPendingExportBundleAssembly` so transient DB or storage errors never crash the daemon; processed bundles emit a single-line summary with assembled/failed counts and idle ticks stay quiet. The explicit `assemble-bundles` CLI remains for one-shot operator use. Engineers running `npm run dev:worker` now get end-to-end "Generate bundle → Download archive (.tar.gz)" without invoking the CLI manually.
